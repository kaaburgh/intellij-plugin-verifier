# Claude Code instructions

The canonical repository instructions are in `AGENTS.md`.

Before editing code:

1. Read the repository-root `AGENTS.md`.
2. Read every nested `AGENTS.md` between the repository root and the files you will modify.
3. Treat the closest applicable `AGENTS.md` as additional, more specific guidance.

Do not duplicate or override those rules here. In particular, preserve the repository's Java/Kotlin compatibility levels, module boundaries, public API discipline, targeted-test workflow and resource/performance invariants described there.
